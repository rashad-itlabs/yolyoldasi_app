name: yolyoldasi
description: >-
  Yol Yoldaşı — şəhərlərarası carpooling tətbiqi: sürücülər boş yerlərini elan
  edir, sərnişinlər marşrut üzrə axtarıb yer bron edir.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+3

environment:
  sdk: ^3.10.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management — one bloc per screen/flow, events in, states out.
  flutter_bloc: ^9.1.1
  bloc: ^9.0.1
  bloc_concurrency: ^0.3.0

  # Networking — the REST API described in API.md.
  dio: ^5.9.0
  flutter_secure_storage: ^9.2.4

  go_router: ^17.5.0
  google_fonts: ^8.2.1
  intl: ^0.20.2
  shared_preferences: ^2.5.5
  equatable: ^2.1.0
  cached_network_image: ^3.4.1
  url_launcher: ^6.3.2
  image_picker: ^1.2.3
  flutter_animate: ^4.5.2
  shimmer: ^3.0.0

  # Notification channels, the Android 13 permission prompt, and drawing the
  # notification while the app is in the foreground — which FCM does not do.
  # Independent of any push transport: it renders, it does not deliver.
  flutter_local_notifications: ^22.3.1

  # No Firebase **Auth**. `POST /auth/firebase` needs an ID token this app can
  # no longer produce, so sign-in takes a Sanctum token directly — see
  # `features/auth/`. An SMS provider will replace that later.
  #
  # This is not a ban on FCM: API.md §5 and §13 are written around it, and
  # `core/services/push/` is the seam a transport plugs into. Adding
  # `firebase_messaging` needs `google-services.json` (Android) and
  # `GoogleService-Info.plist` + an APNs key (iOS) first, or the build breaks.

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^10.0.0
  mocktail: ^1.0.4

  # Generates the iOS/Android launcher icons from `assets/icon/` — see the
  # `flutter_launcher_icons` section at the bottom of this file.
  flutter_launcher_icons: ^0.14.4

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# Launcher icons. Regenerate with `dart run flutter_launcher_icons` after
# changing anything under `assets/icon/`.
#
# `app_icon.png` is the full mark on white — it becomes the iOS icon and the
# legacy Android one.
#
# The adaptive layers carry the mark smaller: launchers crop the outer 18/108 of
# each side, and the generator wraps both layers in a further 16% inset, so the
# mark is drawn at 70% of its source to land inside the safe zone at a size that
# matches the iOS tile. `app_icon_monochrome.png` is the same mark as a black
# silhouette — Android 13 themed icons re-tint it and read only its alpha.
flutter_launcher_icons:
  image_path: "assets/icon/app_icon.png"

  android: "ic_launcher"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"
  adaptive_icon_monochrome: "assets/icon/app_icon_monochrome.png"

  ios: true
  remove_alpha_ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
